import { Server } from 'socket.io';
import { Express } from 'express';
import { ServerInformation } from '../modules/serverInformation';

export default function (io: Server, app: Express, server: ServerInformation) {
    app.get
}
